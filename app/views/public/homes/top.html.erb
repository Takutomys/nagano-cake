     <h4 class="text-center">
     ようこそ、ながのCAKEへ！<br>
　   このサイトは、ケーキ販売のECサイトになります。<br>
　   会員でない方も商品の閲覧はできますが、<br>
     購入には会員登録が必要になります。</h4>
      <div class="container">
        <h1 class="mt-3">新着商品</h1>
        <div class="row">
          <% @items.each do |item| %>
          <div class="col mt-3">
            <% if item.image.attached? %>
              <%= image_tag item.image, size: "100x100" %>
            <% else %>
              <%= image_tag 'no_image', size: "100x100" %>
            <% end %>
            <div class="row">
              <%= item.name %>
            </div>
            <div class="row">
              ￥<%= item.price.to_s(:delimited) %>
            </div>
          </div>
          <% end %>
        </div>
        <div class="row">
          <p class="offset-10 mt-5"><%= link_to '全ての商品を見る＞', items_path %></p>
        </div>
       </div>





